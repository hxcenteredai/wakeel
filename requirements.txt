# =============================================================================
# Wakeel — Python Dependencies
# =============================================================================
# Pinned versions for reproducibility. Update deliberately.
# =============================================================================

# Core LLM client (OpenAI-protocol compatible)
openai==1.54.4

# Agent orchestration with explicit state and loops
langgraph==0.2.45
langchain-core==0.3.21

# API
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.2

# UI
streamlit==1.40.2

# Vector store (local, file-backed)
chromadb==0.5.20

# Document parsing for corpus ingestion
pypdf==5.1.0

# Retry / resilience
tenacity==9.0.0

# Logging / structured output
python-json-logger==2.0.7

# Environment management
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2

# HTTP client for UI -> API
requests==2.32.3
