# Secrets & local environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Vector store (regenerated by ingestion)
data/chroma/

# Copilot registry (runtime artefacts produced by mode=build)
data/copilots/

# Local caches (Slack/CLI tooling, etc.)
.channels_cache.json
.users_cache.json
.claude/

# Cursor IDE — hooks, skills, validation pipeline, reviews
.cursor/

# Other IDE / editor
.idea/
.vscode/
*.swp
*~

# Streamlit local secrets
.streamlit/secrets.toml

# Test / E2E isolation (never commit)
.pytest_chroma/
.e2e_chroma/
.pytest_logs/

# Runtime logs (committed evidence lives under logs/samples/)
logs/run_*.jsonl
logs/llm_calls.jsonl
!logs/samples/

# PO/client source material — kept local only, never committed
PO_requirements/

# Local copy of PO messages at repo root
/PO_message_history/

# Cursor helper script (local workflow only)
scripts/cursor-request-validation.sh

# OS
.DS_Store
