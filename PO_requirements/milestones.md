Milestone 1: Build Mode end-to-end on the Wakeel multi-agent system, from provided scaffold. Accept when ALL pass: (1) LLM connects to Compass; (2) corpus ingested (Labour Law + PDPL), retrieval works; (3) POST /run mode=build returns valid response on a sample input; (4) Streamlit UI on :8001 with chat + audit trail; (5) Loops 1-3 firing in logs; (6) GitLab→GitHub mirror live. Full criteria in Milestone Amendment (sent separately), which controls. Client reviews within 48h.

Milestone 2: Use Mode + delivery on Wakeel. Accept when ALL pass: (1) POST /run mode=use returns verified citations on 3 examples; (2) Loops 4-5 demonstrably triggering in logs (not just coded); (3) NDA copilot works end-to-end; (4) Arabic input works on Interviewer; (5) docker build + run works clean; (6) 3 walkthrough videos in demos/; (7) docs updated; (8) no secrets in repo. Full criteria in Milestone Amendment (sent separately), which controls. Client reviews within 48h.
